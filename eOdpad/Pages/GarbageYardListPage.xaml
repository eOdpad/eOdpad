<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:eOdpad;assembly=eOdpad"
             x:Class="eOdpad.Pages.GarbageYardListPage"
             Title="List" Icon="list_black.png">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <SearchBar Placeholder="Vyhľadať zberný dvor" /> 
            <ActivityIndicator HorizontalOptions="Center" VerticalOptions="Center"
                               IsVisible="False" IsEnabled="True" x:Name="syncIndicator" />
            <local:MyListView x:Name="listView" VerticalOptions="FillAndExpand"
                                   HorizontalOptions="FillAndExpand" IsPullToRefreshEnabled="true" Refreshing="OnRefresh">
                <local:MyListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Company}" Detail="{Binding Address}" />
                    </DataTemplate>
                </local:MyListView.ItemTemplate>
            </local:MyListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>